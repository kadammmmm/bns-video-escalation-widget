import { LitElement } from "lit-element";
export declare const logger: {
    readonly prefix: string;
    error(...msg: any): void;
    info(...msg: any): void;
    warn(...msg: any): void;
};
/**
 * LoginPage component handles the authentication flow for Webex
 *
 * Loading States:
 * - isWebexInitializing: Shows loading spinner while Webex SDK initializes and polls for readiness
 * - isEmailLoading: Shows loading state during email form submission
 * - shouldRenderAgentxApp: Renders the main app when authentication is complete
 */
export declare class LoginPage extends LitElement {
    static get styles(): import("lit-element").CSSResult;
    email: string;
    emailErrorMessage: string;
    isEmailLoading: boolean;
    shouldRenderAgentxApp: boolean;
    isWebexInitializing: boolean;
    private readonly handleEmailChange;
    private readonly handleEmailSubmit;
    private readonly handleEmailKeyPress;
    connectedCallback(): Promise<void>;
    disconnectedCallback(): void;
    private setupLogoutIntegration;
    private readonly handleLogout;
    protected render(): import("lit-element").TemplateResult;
    private renderMainContent;
    private renderLoading;
    private renderEmailFlow;
}
