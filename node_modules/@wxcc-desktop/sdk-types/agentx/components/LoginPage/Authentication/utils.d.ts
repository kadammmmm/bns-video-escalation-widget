import { OAuth } from "@uuip/unified-ui-platform";
import { WebexConfig } from "./types";
/**
 * Validates if the provided email is valid according to Webex backend requirements
 * @param email - The email string to validate
 * @returns boolean indicating if the email is valid
 */
export declare const isValidEmail: (email: string) => boolean;
/**
 * Creates default Webex configuration from environment variables
 * @returns Default Webex configuration
 */
export declare function createWebexConfig(userToken?: OAuth.Token | null): WebexConfig;
export declare function isTokenPresentLocalStorage(): boolean;
