import { OAuth } from "@uuip/unified-ui-platform";
import { WebexConfig, WebexInstance } from "./types";
export declare function initializeWebex(config: WebexConfig): WebexInstance; /**
 * Gets the current Webex instance, initializes if not available
 * @returns Current Webex instance or null if initialization fails
 */
export declare function getWebexInstance(): WebexInstance | null;
/**
 * Initializes Webex with default configuration
 * @returns Promise that resolves with Webex instance
 */
export declare function initializeWebexWithConfig(userToken?: OAuth.Token | null): Promise<WebexInstance>;
/**
 * Checks if Webex is initialized
 * @returns True if Webex is initialized
 */
export declare function isWebexInitialized(): boolean;
export declare function clearWebexInstance(): void;
export declare function getOrCreateWebexInstance(): Promise<WebexInstance>;
