import { WebexInitiateLoginOptions } from "./types";
/**
 * Initiates Webex OAuth login flow
 * @param options - Login options including email, state, and window configuration
 * @returns Promise that resolves when login is initiated
 */
export declare function initiateWebexLogin(options?: WebexInitiateLoginOptions): void;
/**
 * Redirects to Webex login with email hint
 * @param email - User's email address for preauth catalog hinting
 * @param returnTo - Optional return URL to redirect to after login
 */
export declare function redirectToWebexLogin(email: string): void;
/**
 * Removes all parameters from the current URL without triggering a page reload
 */
export declare function removeUrlParameters(): void;
/**
 * Checks if the current page is a callback from Webex authorization
 * @returns True if this appears to be a callback page
 */
export declare function isWebexCallback(): boolean;
export declare function waitForWebexToken(): Promise<void>;
export declare function isTokenPresentInWebex(): Promise<boolean>;
