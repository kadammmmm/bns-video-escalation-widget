import { Signal } from "@uuip/unified-ui-platform-sdk";
import { createDataNotifsAiAssistantEvents } from "./ai-assistant-events";
export interface DataNotifsEventSubscription {
    eventType: string;
    callback: (data: unknown) => void;
    filter?: Record<string, unknown>;
}
/**
 * DataNotifsService handles real-time data notification connections through window.wxcc.rtdwc
 */
export declare class DataNotifsService {
    private readonly subscriptions;
    private readonly customNotifHandlers;
    onDataNotifsEvent: Signal.WithData<{
        eventType: string;
        data: unknown;
    }>;
    private readonly onDataNotifsEventSend;
    private connectionInitialized;
    private onReadyCallbacks;
    readonly aiAssistant: ReturnType<typeof createDataNotifsAiAssistantEvents>;
    /**
     * Initialize Data Notifs Service - uses window.wxcc.rtdwc directly
     */
    constructor();
    setConnectionInitialized(isInitialized: boolean): void;
    onReady(callback: () => void): void;
    isReady(): boolean;
    private ensureReady;
    subscribe(subscription: DataNotifsEventSubscription): void;
    unsubscribe(eventType: string): void;
    registerCustomNotifHandler(messageType: string, handler: (data: unknown, messageType: string) => void): void;
    unregisterCustomNotifHandler(messageType: string, handler?: (data: unknown, messageType: string) => void): void;
    cleanup(): void;
}
//# sourceMappingURL=data-notifs-service.d.ts.map